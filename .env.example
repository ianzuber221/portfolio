# OpenAI API key powers the recruiter personalization + project summaries.
# The site runs fine without it and falls back to curated static content.
OPENAI_API_KEY=your-openai-key

# Optional: model override for OpenAI calls (defaults to gpt-4o-mini).
OPENAI_MODEL=gpt-4o-mini
